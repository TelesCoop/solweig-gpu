[project]
name = "solweig-lyon"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "geopandas",
    "laspy[lazrs]",
    "numba>=0.65.1",
    "numpy",
    "pandas",
    "pyproj",
    "rasterio",
    "requests",
    "scipy",
    "solweig-gpu",
]

[tool.uv.sources]
solweig-gpu = { path = "../SOLWEIG-GPU", editable = true }
